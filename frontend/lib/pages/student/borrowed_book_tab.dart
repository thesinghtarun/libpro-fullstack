import 'package:flutter/material.dart';

class BorrowedBookTab extends StatelessWidget {
  const BorrowedBookTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("borrowed book"),
    );
  }
}
